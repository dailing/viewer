# M7: chat 数据面全量化（raw 事件 + 解析块）

## 背景与已定决策（不要重新讨论）

工作区：`/mnt/oldroot/home/d/Sync/viewer-plugin`（branch `plugin-rewrite`），Go 在 `~/.local/go`（1.26.5，先 `export PATH="$HOME/.local/go/bin:$PATH"`）。DB 仅 GORM + glebarez/sqlite，禁 CGO。不向后兼容，但 AutoMigrate 必须对现有 chat.sqlite3 无损（只加表/加列）。

用户拍板的四项决策：

1. **接力（relay）维持现状**：`internal/plugins/chat/runtime.go` 的 `runRelay` 插件内顺序执行就是最终形态。不做 worker 队列/lease/failover，内核与框架永远不知道接力的存在。本里程碑**不改接力代码**。
2. **原始事件全量保留**：ACP / codex app-server 两个 driver 的**每一条 session update 通知**都要原文落库，以后任意再加工都从这里来。解析结果**单独存**另一张表，不与原始帧混在一起。
3. **codex 只留 app-server 版本**：`internal/codexserver/` + `codex_agent.go` 是唯一 codex 路径。旧 codex-acp 适配器永远不移植。本里程碑核实全仓库无旧路径残留引用（代码/文档/脚本），有则清除。
4. **summary / ctx builder 已存在**（`summary.go`、`runtime.go` 的 `buildNewSessionContext`/`buildRoleSwitchBridge`/`historyPrompt`），本里程碑把 summary 的 transcript 数据源扩展到解析块；预算继续用词数近似，**不做 token 精确化**。

## 现状问题（已核实，直接照此修）

- `runtime.go:361 handleUpdate` → `updateText` 只提取 `agent_message_chunk` 文本，**其余一切 update（tool_call、file_change、thinking、command 等）直接丢弃**。
- `internal/acp/client.go:20` 的 `Update` 已带 `Raw json.RawMessage`（完整 envelope params）——raw 已经有了，只是没人存。
- `internal/codexserver/client.go` 的 `Update` **没有 Raw 字段**；`codex_agent.go` 的 `OnUpdate` 只翻译 `item/agentMessage/delta`，其余 method 全部丢弃。
- 参考语义：生产版 `~/.view/agent-history.sqlite3` 的 `super_workspace_messages` 表——每事件一行，`event_index`/`event_type`/`text`/`patch_text`/`raw_json` 全量，`UNIQUE(provider, viewer_session_id, source_event_id)` 幂等。

## 任务

### 1. raw 事件表 `turn_events`（新表，append-only）

字段（GORM model 放 `internal/plugins/chat/store.go`，命名跟现有风格）：

- `id` primary key（newID()）
- `chat_id` index not null；`turn_id` index not null；`role_id` index
- `provider` not null（`hermes` / `codex-app-server`）
- `session_id` not null（provider 侧 session/thread id）
- `seq` int not null——**每个 turn 内单调递增**，由插件在 handleUpdate 路径分配（内存计数器即可，turn 开始置 0）
- `kind` not null——driver 原始判别：ACP 取 `sessionUpdate`（或 `session_update`）值，缺省 `unknown`；codex 取 method 名
- `raw_json` not null——**完整原始帧原文**（ACP = `Update.Raw`；codex = 见任务 2 新增的 Raw）
- `occurred_at` int64 index（毫秒，与现有表一致）

写入时机：`handleUpdate`（及 codex 等价路径）里**任何过滤之前**先落 `turn_events`，落库失败只记日志、不阻断 turn。删 chat 时级联删除（`deleteChat` 的事务列表加 `&TurnEvent{}`、`&MessageBlock{}`）。

### 2. codexserver 全量事件透传

- `codexserver.Update` 增加 `Raw json.RawMessage`（`handleMessage`/`readLoop` 里保留原始消息原文，照 `acp/client.go:290` 的 copy 写法）。
- `codex_agent.go` 的 `OnUpdate` **不再丢弃非 delta 事件**：所有 update 都转成内部事件上送。做法自定，但推荐：chat 插件内部定义统一 `driverEvent{Provider, SessionID, Kind, Raw, Text}`，`acpClient` 与 `codexAgent` 都产出它；`Text` 仅供现有 Message 文本路径用（codex 仍只有 `item/agentMessage/delta` 出文本），`Kind`/`Raw` 供 `turn_events` 落库。`agent` 接口按需调整，保持 mock agent（chat_test.go 的 fakeAgent、scripts 里的 mock ACP agent）同步适配。

### 3. 解析块表 `message_blocks`（新表，解析结果单独存）

从每条 `turn_events` 行**同步派生**（同一 ingest 点解析，解析失败只记日志、不丢 raw 行）：

- `id` primary key；`event_id` index not null（→ turn_events.id）；`chat_id`/`turn_id` index
- `kind` not null——归一化枚举：`agent_text` / `thinking` / `tool_call` / `tool_result` / `file_change` / `command` / `other`
- `text`（该块的可读文本，没有则空）
- `payload` ——JSON object，归一化字段：tool_call → `{name, arguments?}`；file_change → `{path, patch?}`；command → `{command, status?, output?}`；其余从 raw 能拿什么拿什么，拿不到就 `{}`
- `occurred_at` int64

ACP 解析映射（对拍生产版 `backend/app/super_workspace.py` 与 ACP 协议）：`agent_message_chunk`→agent_text、`agent_thought_chunk`→thinking、`tool_call`/`tool_call_update`→tool_call（含 name/status）、`plan`→other、其他→other。codex app-server 映射：`item/agentMessage/delta`→agent_text、`item/reasoning/delta`(或 reasoning 系)→thinking、`item/commandExecution/*`→command、`item/fileChange/*`（或 patch 系）→file_change、`item/toolCall/*`→tool_call；**method 名以实际抓到的为准，拿不准的进 `other` 并在 payload 里带 method 名**——宁可 other 不可丢。

`Message` 表与 `chat:{id}:message` 事件**行为不变**（仍是用户可见文本视图）。

### 4. summary transcript 扩展到解析块

`summary.go` 生成 transcript 时，在 turn 的消息之间按时间插入块的一行式摘要：tool_call → `[tool: <name> <status>]`，file_change → `[file: <path>]`，command → `[cmd: <command> → <status>]`。thinking 块不进 transcript。词数预算逻辑不变。

### 5. 迁移脚本扩展

`scripts/migrate_agent_history.py`（M6c 产物）增加：把生产版 `super_workspace_messages` 的每行映射进 `turn_events`（raw_json 原文、provider/session_id/event_type→kind、occurred_at 换算毫秒、turn_id 已有列直接用；seq 用 event_index），**幂等**（以生产版 `id` 做目标表主键或唯一键去重），源库只读/backup 打开不变。`message_blocks` 不迁（可随时从 raw 重解析，脚本里注明）。fixture 自测同步扩展：造带 raw_json 的 fixture 行，断言迁后 turn_events 行数/原文一致、重跑幂等。

### 6. 文档

- `architecture.md`：chat 插件段加 turn_events/message_blocks 两表与事件流（driver → raw 落库 → 解析块 → Message 文本视图）的描述。
- **不要动** `docs/plugin-framework.md`（框架文档由 Hermes 逐节定稿）。

## 验收门禁（全部亲自跑通再报告）

1. `go build ./...` 且 `go vet ./...`
2. `go test -race ./internal/plugins/chat/ ./internal/acp/ ./internal/codexserver/`（其余包 `go test ./...` 不红）
3. `bash scripts/smoke_all.sh` 保持 **9/9**，并在 `smoke_chat.py` 里新增断言（用 mock ACP agent 发 tool_call 类 update）：turn 结束后 `turn_events` 行数 ≥ 实际发送 update 数、raw_json 可 JSON round-trip、`message_blocks` 有对应 kind 行
4. 迁移脚本 fixture 自测 PASS（含 turn_events 幂等重跑）
5. 报告里附：新表 schema、一条真实落库的 raw_json 样例（mock 产生）、派生块样例

## 明确禁做

- 不做前端块渲染（数据面先行，前端后面单开）
- 不改接力/路由/dispatch 逻辑；不加 worker 队列
- 不做 token 精确预算；不做 Hindsight retention 回写
- 不引入 CGO、不引入新 driver（opencode 依然不实现）
