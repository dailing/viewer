# M8a: agent 插件拆分第一波——agentdriver 共享包 + viewer.agent-hermes + chat 走总线契约

## 背景与已定决策（不要重新讨论）

工作区 `/mnt/oldroot/home/d/Sync/viewer-plugin`（branch `plugin-rewrite`）。Go 在 `~/.local/go`（先 `export PATH="$HOME/.local/go/bin:$PATH"`）。DB 仅 GORM + glebarez/sqlite，禁 CGO。权威设计：`docs/plugin-framework.md` **v0.23**（刚定稿，先读它的 v0.23 变更行 + §16-6 + A.7），核心决策：

1. **agent 实现拆为独立 headless 插件族**：`viewer.agent-hermes`（ACP）/ `viewer.agent-codex`（app-server，M8b 才拆）/ `viewer.agent-opencode`（M8c 新建）。无 UI、单实例（instance 恒 `_`）、编译期 registry 随 viewerd 启动，插件内部自管 session→子进程池。
2. **统一总线契约**（chat 只认契约不认实现）：
   - RPC `<plugin>:_:start`：`{cwd, target:{agent,provider,model,parameters}, session_id?}` → `{session_id, resumed}`；`parameters` opaque map 透传，agent 插件自行解释
   - RPC `<plugin>:_:prompt`：`{session_id, text}` → **立即 ack**，turn 异步执行
   - RPC `<plugin>:_:cancel`：`{session_id}` → `{stopped}`
   - 事件 `<plugin>:_:event`：`{session_id, seq, kind, raw_json, block:{kind,text,payload}}`——raw 原文与已解析块同帧；`seq` 由 agent 侧 turn 内单调发号（turn 开始置 0）
   - 事件 `<plugin>:_:turn-ended`：`{session_id, stop_reason}`
   - retained mailbox `<plugin>:_:catalog`：`{agent, providers:[{provider, models[], parameter_schema?}]}`，插件启动即 set
3. **chat = 纯编排**：roles/routing/relay/DB/summaries 不动；provider 解析改走 **routing policy**（既有 `RoutingPolicyConfig`/`RoutingCandidateConfig` 模型，`models.go` 已有）：role 挂 `routing_policy_id`，candidates 有序 (agent, provider, model, parameters)，turn 开始按序解析首个 `enabled` 且对应 agent 插件在线（catalog mailbox 存在）的 candidate；`role.provider/model` 降为迁移输入——role 无 policy 时运行时由 provider/model 合成单 candidate（等价生产版 `_ensure_role_routing_migration` 的语义，但 M8a 只做运行时合成，不回写配置）。`auto_failover` 基本语义：start/prompt RPC 失败时按序试下一个 candidate，`max_attempts` 封顶（0/未设 = 1 次）。
4. provider → 插件映射：C1 `plugins.viewer-chat.agents`，默认 `{hermes: "viewer.agent-hermes", "codex-app-server": "viewer.agent-codex"}`（codex 插件 M8b 才存在，映射先放着）。

## 现状（已核实，照此动手）

- `internal/plugins/chat/`：`runtime.go` 的 `ensureRuntime`（spawn `hermes acp` 子进程、session 三元组复用）、`runRelay`（顺序接力，同步 `Prompt` 等 turn 结束）、`handleUpdate`（落 `turn_events`/`message_blocks`/文本消息）、`events.go`（`driverEvent` + `deriveACPBlock`/`deriveCodexBlock`）、`codex_agent.go`（codex 走 `internal/codexserver`，M8b 才动它）。`agent` 接口在 chat 包内（Initialize/NewSession/LoadSession/Prompt/Cancel/Close + OnUpdate）。
- `internal/acp/`：ACP stdio client，`Update` 已带 `Raw`。
- mock 测试设施：`scripts/mock_acp_agent.py`（经 `VIEWER_HERMES_COMMAND` 注入）；`chat_test.go` 的 `fakeAgent`。冒烟 `scripts/smoke_chat.py` 断言 turn_events/message_blocks/接力等全链路。
- 插件注册：`internal/pluginapi` 编译期 registry（参考 terminal/inspector 插件的注册方式）。

## 任务

### 1. `internal/agentdriver` 共享包（新建）

契约类型 + 常量，供 chat 与所有 agent 插件共用：`Target{Agent,Provider,Model string; Parameters map[string]any}`、`Block{Kind,Text,Payload string}`（Payload 为 JSON string，与 `message_blocks` 表对齐）、`EventFrame{SessionID string; Seq int; Kind string; RawJSON string; Block Block}`、kind 常量（`agent_text`/`thinking`/`tool_call`/`tool_result`/`file_change`/`command`/`other`）、`Catalog{Agent string; Providers []ProviderCatalog}`。把 `deriveACPBlock` 从 chat `events.go` 挪进 `internal/acp`（导出 `ParseBlock(kind string, data map[string]any)`）；`deriveCodexBlock` 暂留 chat（M8b 挪）。

### 2. `viewer.agent-hermes` 插件（`internal/plugins/agenthermes/`，新建）

- goroutine 插件，编译期注册进 viewerd（照 terminal 插件的注册路径）；插件 id `viewer.agent-hermes`。
- 内部搬移 chat 的 hermes 路径：spawn `hermes -p <profile> --yolo acp` 子进程（env 覆盖 `VIEWER_HERMES_COMMAND/PROFILE/YOLO` 语义不变，改由本插件读）、session 池（session_id → 子进程 + ACP client）、`LoadSession` 恢复。
- 实现统一契约三个 RPC + 两个事件 + catalog mailbox。事件流：ACP client 的每条 update → 组 `EventFrame`（`kind` 取 sessionUpdate 值、`raw_json` = `Update.Raw` 原文、`block` = `acp.ParseBlock` 结果）→ publish `<plugin>:_:event`；turn 结束（ACP prompt 请求 resolve）→ publish `:<_>:turn-ended`（stop_reason 对齐现有 `end_turn`/`cancelled`/`error`）。`seq` turn 内从 0 递增。
- catalog：best-effort 从 `~/.hermes/config.yaml` 读 models/providers（文件不存在或解析失败不致命，落到内置默认 `{agent:"hermes", providers:[{provider:"default", models:[]}]}`）；C1 `plugins.viewer-agent-hermes.catalog` 可整体覆盖。**不为 catalog 发明新机制。**
- 单测：用 mock ACP agent（`scripts/mock_acp_agent.py`）驱动 start/prompt/event/cancel 全路径。

### 3. chat 插件改走总线契约

- 新增 bus 侧 provider 句柄：按 candidate.agent 查 C1 agents 映射得插件 id → RPC start/prompt/cancel；订阅该插件 `:_:event` / `:_:turn-ended`（按 session_id demux 到 activeTurn）。落库逻辑（`handleUpdate` 的 turn_events/message_blocks/messages 三层）**保持不变**，只是数据源从进程内回调换成总线事件帧（block 直接用帧里解析好的，不再自己 derive——hermes 路径）。
- `runRelay` 改造：turn 开始 → policy 解析出 candidate → start（带 RoleSession 里存的 session_id 尝试恢复）→ prompt（ack 后等 turn-ended 事件定 stop_reason）；`stop` RPC 的取消路径改走 agent 插件 cancel。chat 内 `agent` 接口与 codex 进程内路径**保留不动**（M8b 移除），hermes 进程内路径删除。
- `chat:_:agent-catalog` RPC（新增）：聚合 C1 agents 映射里各插件的 catalog mailbox 返回给前端（插件离线则标 `online: false` 不报错）。
- RoleSession 表结构不变（`provider` 列继续存 candidate 的 agent/provider 标识，用于恢复匹配）。

### 4. 冒烟与测试

- `smoke_chat.py` 升级：mock ACP agent 现在被 agent-hermes 插件 spawn（env 注入路径不变）；断言全部保留（turn_events/message_blocks/接力/幂等 stop），新增：①catalog mailbox 已 set 且 agent=hermes；②给 role 配 routing_policy（candidate 指 hermes）后 dispatch 走通；③`chat:_:agent-catalog` 返回聚合结果。冒烟总数维持 9/9（断言加在 chat suite 里）。
- `chat_test.go`/`agenthermes_test.go`：fakeAgent 机制适配新边界；race 必须绿。

### 5. 文档

- `architecture.md`：chat 段改写（纯编排 + 总线契约消费），新增 agent-hermes 插件段与 `internal/agentdriver` 包说明。
- **不要动** `docs/plugin-framework.md`（Hermes 管）。

## 验收门禁（全部亲自跑通再报告）

1. `gofmt -l .` 空、`go build ./...`、`go vet ./...`
2. `go test -race -count=1 ./internal/plugins/chat/ ./internal/plugins/agenthermes/ ./internal/acp/ ./internal/agentdriver/` + `go test ./...` 全绿
3. `bash scripts/smoke_all.sh` = **9/9**（chat suite 含新增三条断言）
4. 报告附：catalog mailbox 实际值样例、一条总线 event 帧样例（mock 产生）、policy 解析路径的冒烟输出

## 明确禁做

- 不拆 codex（M8b）、不做 opencode（M8c）、不做前端 catalog 选择器 UI（M8b）
- 不改 relay/routing 语义；role.provider/model 迁移不回写配置（运行时合成即可）
- 不加新框架机制（发现就走 C1 映射 + catalog mailbox）；不引入外部依赖；不动 `turn_events`/`message_blocks` schema
