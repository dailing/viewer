# M8b: agent 插件拆分第二波——viewer.agent-codex + failover 完整语义 + 前端 catalog 选择器

## 前置

M8a 已完成（先读 `.hermes-task-m8a-agent-split.md` 与 `git log` 确认落点）：`internal/agentdriver` 契约包、`viewer.agent-hermes` 插件、chat 已改走总线契约 + policy 解析。本里程碑把最后一个进程内 agent 路径（codex）切出去，chat 内的 `agent` 接口与 `codex_agent.go` **整体删除**。

工作区 `/mnt/oldroot/home/d/Sync/viewer-plugin`（branch `plugin-rewrite`）。Go 在 `~/.local/go`。禁 CGO。权威设计 `docs/plugin-framework.md` v0.23（不要动它）。

## 任务

### 1. `viewer.agent-codex` 插件（`internal/plugins/agentcodex/`，新建）

- 把 chat 的 `codex_agent.go` 搬出：spawn `codex app-server --stdio`（env `VIEWER_CODEX_APP_SERVER_ENABLED/COMMAND/YOLO` 语义不变，改由本插件读），`internal/codexserver` client 维持共享包。
- 实现 M8a 同一套统一契约：start/prompt/cancel RPC + `:_:event` / `:_:turn-ended` + catalog mailbox。thread → session 映射照旧（ThreadStart/ThreadResume）。
- 解析块：`deriveCodexBlock` 从 chat `events.go` 挪进 `internal/codexserver`（导出 `ParseBlock(method string, data map[string]any)`），映射规则不变（agentMessage/delta→agent_text、reasoning→thinking、commandExecution→command、fileChange/patch→file_change、toolCall→tool_call、toolResult→tool_result、拿不准→other 带 method 名）。
- catalog：默认 `{agent:"codex", providers:[{provider:"openai-subscription", models:[...]}]}`——models 尽力而为（`codex` CLI 可查则查，查不到给空列表不致命）；C1 `plugins.viewer-agent-codex.catalog` 可整体覆盖。
- 单测走 `scripts/mock_codex_server.py`（M6c 产物），不起真 codex。

### 2. chat 删除进程内 agent 路径

- 删 `codex_agent.go`、`events.go` 的 codex 分支、`agent` 进程内接口及其全部调用点；所有 provider 消费统一走 M8a 的总线句柄。
- C1 `plugins.viewer-chat.agents` 默认映射此时两个都生效：`hermes → viewer.agent-hermes`、`codex-app-server → viewer.agent-codex`。
- chat 不再 import `internal/acp`、`internal/codexserver`。

### 3. failover 完整语义

`auto_failover: true` 的 policy：start 或 prompt RPC 失败、或 turn-ended 携带 `error` 时，按序试下一个 enabled candidate，`max_attempts` 封顶（0 = 1 次）；每次尝试在 turn 的事件流里留痕（chat 侧记一条系统消息或在 turn-completed payload 带 `attempts` 明细——选简单那个，报告里说明）。`auto_failover: false` 保持现状（首个 candidate 失败即 turn failed）。

### 4. 前端 catalog 选择器（`next/frontend/src/plugins/chat/`）

- RolesPanel/RoutesPanel 的 candidate 编辑：agent/provider/model 从 `chat:_:agent-catalog` RPC 拉取做下拉（三级联动：agent → provider → model），离线 agent 置灰；parameters 暂用 JSON 文本框。
- 紧凑图标按钮风格不变；不自动滚动。
- ts-sdk 契约测试（`next/ts-sdk/tests/frontend-contract.test.ts`）加 catalog RPC 的契约断言（对真实 viewerd）。

### 5. 冒烟与文档

- `smoke_chat.py` 增加 codex 路径端到端（mock codex server 经总线契约：start→prompt→event 落库→turn-ended），以及 failover 断言（policy 两个 candidate，第一个指向不存在/会失败的 target，验证落到第二个）。
- 冒烟总数 9/9 保持（断言进 chat suite）。race 绿。
- `architecture.md` 更新（agent-codex 插件、chat 纯编排终态）。不动 `docs/plugin-framework.md`。

## 验收门禁

1. `gofmt -l .` 空、`go build ./...`、`go vet ./...`
2. `go test -race -count=1 ./internal/plugins/... ./internal/acp/ ./internal/codexserver/ ./internal/agentdriver/` + `go test ./...` 全绿
3. `bash scripts/smoke_all.sh` = 9/9（含 codex 总线路径 + failover 断言）
4. `cd next/ts-sdk && npm test` 全绿（含 catalog 契约）；`cd next/frontend && npx vue-tsc --noEmit && npx vite build` 过
5. 报告附：chat 已无 `internal/acp|codexserver` import 的 grep 证据、failover 尝试的冒烟输出

## 明确禁做

- 不做 opencode（M8c）；不改 relay；不动 turn_events/message_blocks schema；不加框架机制
