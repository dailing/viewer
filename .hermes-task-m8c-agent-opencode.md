# M8c: viewer.agent-opencode——ACP 抽象的第二租户

## 前置

M8a/M8b 已完成（读 `.hermes-task-m8a-agent-split.md` / `.hermes-task-m8b-agent-split.md` 与 `git log`）：统一 agent 总线契约已在 hermes（ACP）与 codex（app-server）两个插件上跑通，chat 纯编排。本里程碑新建第三个 agent 插件，验证 ACP 抽象能不加修改地承载第二个 provider。

工作区 `/mnt/oldroot/home/d/Sync/viewer-plugin`（branch `plugin-rewrite`）。Go 在 `~/.local/go`。禁 CGO。权威设计 `docs/plugin-framework.md` v0.23（不要动它）。

## 任务

### 1. 调研生产版 opencode 驱动（先读后写）

读 worktree 根 `backend/app/opencode_*.py`（生产版参考实现，只读不动）：搞清 spawn 命令、参数、session 建立/恢复语义、与 hermes ACP 路径的差异点。报告里给出差异清单。若生产版 opencode 驱动就是 ACP 方言，本插件应当是 `viewer.agent-hermes` 的近亲：复用 `internal/acp` client 与 `acp.ParseBlock`，只换 spawn 与 session 适配层。

### 2. `viewer.agent-opencode` 插件（`internal/plugins/agentopencode/`，新建）

- goroutine 插件，编译期注册；插件 id `viewer.agent-opencode`。
- spawn 命令经 env 覆盖：`VIEWER_OPENCODE_COMMAND`（默认 `opencode`）、`VIEWER_OPENCODE_ARGS`（默认按生产版调研结论）。子进程池 + session 管理照 agent-hermes 的模式。
- 完整实现统一契约：start/prompt/cancel RPC + `:_:event` / `:_:turn-ended` + catalog mailbox（`{agent:"opencode", providers:[...]}`，C1 `plugins.viewer-agent-opencode.catalog` 可覆盖）。
- ACP update 的 kind/block 解析直接复用 `acp.ParseBlock`；opencode 有 hermes 没有的 update 类型时，进 `other` 并在 payload 带原始类型名（"宁可 other 不可丢"原则不变）。
- **验收关键点（第二租户检验）**：`internal/acp` 包与契约类型**零修改**或仅有泛化性修改（如发现 hermes 硬编码，泛化之，不许加 opencode 特判分支）。

### 3. chat 侧接入

- C1 `plugins.viewer-chat.agents` 默认映射加 `opencode → viewer.agent-opencode`。
- chat 本体**应零改动**——若发现必须改 chat 才能接入，说明 M8a 的契约泄漏了 hermes 假设，停下修契约（在报告里标注）。

### 4. 测试与冒烟

- mock：`scripts/mock_opencode_agent.py`（照 `mock_acp_agent.py` 的 ACP 协议壳，按需加 opencode 方言 update 类型），经 `VIEWER_OPENCODE_COMMAND` 注入；绝不 spawn 真 opencode。
- `smoke_chat.py` 增加 opencode 端到端路径（policy candidate 指 opencode → dispatch → event 落库 → turn-ended）；冒烟总数 9/9 保持。
- race 绿；`go test ./...` 全绿。

### 5. 文档

- `architecture.md` 加 agent-opencode 段。不动 `docs/plugin-framework.md`。

## 验收门禁

1. `gofmt -l .` 空、`go build ./...`、`go vet ./...`
2. `go test -race -count=1 ./internal/plugins/agentopencode/ ./internal/acp/ ./internal/agentdriver/` + `go test ./...` 全绿
3. `bash scripts/smoke_all.sh` = 9/9（含 opencode 路径）
4. 报告附：与生产版 opencode 驱动的差异清单、`internal/acp` 零修改（或泛化修改说明）的证据、opencode 路径冒烟输出

## 明确禁做

- 不改 chat 编排逻辑；不改 relay；不做 UI；不加框架机制；不对 hermes 插件行为做任何回归性修改
