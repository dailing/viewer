# M9: Shell 语义修正 + chat-manager 前端拆分 + turn_id 贯穿 + roles/routes 存储迁移

> 任务规格（Hermes 编写，Codex 执行）。权威设计：docs/plugin-framework.md v0.24（header 变更行 + §8.7 + A.7，已提交 ba8e0a2）。先读它，再读本规格列出的代码现状。

## 已核实现状（verified facts，不要重新调查，直接信任）

### 前端（next/frontend/，Vue3 + Pinia + TS）
- `src/stores/layout.ts`：`openInstance(paneType, instanceId)`（line ~101）——目标已开则聚焦；**否则直接覆盖 active pane 的 content**（这是 bug 根源）。`splitPane(paneId, direction)` 已存在：新空 pane 占 second slot 并聚焦。`SplitDirection` 注释：`"horizontal"` = 分隔线水平 → 子 pane 上下堆叠；`"vertical"` = 左右分。
- `src/shell/Dock.vue`：singleton provider 的 dock 条目**只在 `layout.isUidOpen(type:main)` 为真时显示**（line ~31-43）——pane 被顶掉后图标消失。`createFrom`：singleton → `openInstance(type, "main")`；非 singleton → `provider.create?.()`。
- `src/shell/definePlugin.ts`：`PluginModule = {id, components?: Record<string, Component>, createDockProvider?, activate?, deactivate?}`；`DockProvider = {type, icon, title, singleton?, instances: DockInstance[], create?, remove?}`。**一个插件一个 DockProvider（一个 dock 分区），components 数量不限。**
- `src/plugins/chat/index.ts`：注册 4 种组件（chat=ChatPane / chats=ChatsPanel / roles=RolesPanel / routes=RoutesPanel）；DockProvider 的 instances 来自 RPC `chat:_:chats:list`（label 已含 `★ ` pinned 前缀 + active_chat_id → state "running"），**没有 `create` 方法**（"+" 菜单对 chat 静默无效——入口死锁：ChatsPanel 只能从已有 ChatPane 头部进，没 chat 就没有 ChatPane）。
- `src/plugins/terminal/index.ts`：DockProvider 有 create/remove，instances 来自 `terminal:*:status` mailbox + `terminal:_:list`。
- 前端契约测试模式：`next/ts-sdk/tests/frontend-contract.test.ts`（驱动真实 viewerd 验证 pane 插件），新 pane 复用这个模式。

### 后端（next-go/，Go 1.26.5 at ~/.local/go/bin，GORM + glebarez/sqlite，禁裸 SQL 禁 CGO）
- `internal/plugins/terminal/terminal.go`（line ~633）：spawn 用 `exec zsh -f`——**`-f` 显式跳过全部 rc 文件**（.zshrc/oh-my-zsh 不加载）。
- `internal/plugins/chat/`：chat.go / runtime.go / router.go / store.go / models.go / config.go / agent_bus.go / llm.go / summary.go / hindsight.go。roles/routing policies 目前在 **C1 config-store**（`plugins.viewer-chat.*`）；chats/messages/turns/turn_events/message_blocks/role_sessions 在插件 DB（chat.sqlite3，GORM AutoMigrate）。
- `internal/agentdriver/`：chat↔agent 统一契约包。当前事件帧只有 `session_id+seq`，chat 侧用 session→turn 映射解复用。
- `internal/plugins/agenthermes|agentcodex|agentopencode/`：三个 headless agent 插件，实现统一契约 RPC `start`/`prompt`/`cancel` + 事件 `event`/`turn-ended` + retained mailbox `catalog`。
- 生产版参照（只读参考，勿改）：`/mnt/oldroot/home/d/Sync/viewer/backend/app/agent_history.py` line 395-481——`SuperWorkspaceChatRow`（name/type/root/**common_prompt**/**member_role_ids_json**/role_routing_policy_overrides_json）、`SuperWorkspaceRoleRow`（name/description/**prompt**/provider/cwd/model/routing_policy_id/session_policy/context_recycle_percent/tokens）。dispatch = LLM router 用 `dispatch_prompt_template` 在 member roles 中选 role（`super_workspace.py` `_render_dispatch_prompt`），role 的 run prompt = `common_prompt + role.prompt` 拼接（`super_workspace_runtime.py` line ~325-332）。

## 工作项

### W1 — shell：openInstance 不再顶人（layout.ts）
新语义（§8.7）：目标 uid 已开 → 聚焦；active pane 空 → 用之；有其他空 pane → 用第一个空 pane；都没有 → 对 active pane `splitPane(activePaneId, "vertical")`（新 pane 在右）后放入内容。**已占用 pane 的内容永不被静默替换。** 注意保持 `closePane`/`refreshPane` 等行为不变。

### W2 — shell：dock singleton pin 制（Dock.vue + definePlugin.ts）
- singleton 条目默认 **pinned**：无论 pane 是否开着都显示在 dock；点击 = 打开或聚焦（走 W1 新语义）。
- hover 显示 pin/unpin 切换按钮（参照现有 dock-remove 的 hover 模式）；unpin 后恢复"开着才显示"。
- pin 状态持久化 localStorage（key 按 provider.type），默认 pinned。
- 非 singleton 行为不变。

### W3 — terminal 登录 shell（terminal.go）
`exec zsh -f` → `exec zsh -l`（login shell：读 .zprofile + .zshrc，oh-my-zsh 正常加载）。如果代码里对 bash/sh 有类似分支，同样改 login 语义（`bash -l` 等）。目标：插件开的终端与用户直接开的终端环境一致。

### W4 — turn_id 贯穿（agentdriver + 三个 agent 插件 + chat）
- 契约变更：`prompt` payload 必带 `turn_id`；`start` 在隐式首 turn 时携带 `turn_id?`；`event`/`turn-ended` 帧 payload **echo `turn_id`**。
- chat 侧：turn id 已由 chat 生成（DB Turn 行）——把它传下去，事件按 `turn_id` 解复用，**删除 session→turn 映射**（同 session 连续两 turn 的歧义消除）。
- agent 插件侧：echo 即可，不理解语义。`internal/acp`、`internal/codexserver` 协议库不动（turn_id 是插件帧 payload 字段，不是 provider 协议字段）。
- 三个 agent 插件的测试同步更新（echo 断言）。

### W5 — roles/routing policies 存储迁移 C1 → 插件 DB（chat 插件）
- 新 GORM 表（chat.sqlite3，AutoMigrate）：
  - `roles`：id / name / description / prompt / cwd / routing_policy_id / session_policy / context_recycle_percent（nullable）/ context_recycle_tokens（nullable）/ created_at / updated_at。**不带 provider/model 直写字段**（v0.23：candidate 在 policy 里）。
  - `routing_policies`：id / name / candidates_json（有序 `[{agent, provider, model, parameters, enabled}]`）/ auto_failover / max_attempts / created_at / updated_at。
- 启动一次性迁移：若两表为空且 C1 `plugins.viewer-chat` 下有遗留 roles/routes 配置 → 读出来插入 DB（幂等，迁完不再读 C1 的这些键）。
- C1 收缩为插件级配置：`agents` 映射、LLM router、预算等保留；roles/routes 键删除。
- router.go / runtime.go / agent_bus.go 全部改从 DB 读 roles/policies，行为不变（LLM router、failover 语义、relay 都不动）。
- RPC 面：`chat:_:*` 下 roles/routes 的 list/create/update/delete 改走 DB（前端面板继续用同一批 channel 名，尽量不改前端调用名）。

### W6 — chat-manager 前端插件 + chat 入口闭环
- **新前端插件 `src/plugins/chat-manager/`**：singleton DockProvider（type `"chat-manager"`，title "聊天管理"，图标自选 bootstrap-icons），单个 pane 组件 `ManagerPanel.vue`，**三个 tab**：
  1. **聊天**：chat 列表（pinned 排序在前）、新建（名字/root/类型）、重命名、删除、pin 切换；编辑 `common_prompt`；**member roles 管理**（从 roles 列表把角色拉进 chat / 移出，对齐生产版 member_role_ids_json 语义）。
  2. **Roles**：role CRUD——name/description/**prompt**（run prompt）/cwd/session_policy/context 回收阈值/**routing_policy_id 绑定**；provider/model 选择用 **catalog 三级选择器**（agent → provider → model，数据来自现有 `chat:_:agent-catalog` RPC）辅助构造/编辑 policy candidates。
  3. **路由**：routing policy CRUD——有序 candidate 列表（agent/provider/model/parameters/enabled，可上下移）、auto_failover、max_attempts。
  - ChatsPanel/RolesPanel/RoutesPanel 的现有实现搬过来做底子（可以从 chat 插件目录移动文件并改 import），但合并进一个带 tab 的 ManagerPanel。
- **chat 前端插件瘦身**：`src/plugins/chat/` 只留 `chat` 组件（ChatPane）；chats/roles/routes 组件移到 chat-manager。DockProvider 加 `create`：调后端建默认 chat → refresh → `openInstance("chat", newId)` 直接打开（快速路径，管理走 chat-manager）。
- ChatsPanel 原有"从 ChatPane 头部进入"的按钮可以保留（改为打开 chat-manager singleton），也可以删——以入口闭环为准：dock 有常驻"聊天管理"图标（W2 pin 制保证），"+" → 聊天能建第一个 chat。
- 前端契约测试：给 chat-manager pane 加 `frontend-contract.test.ts` 风格的用例（驱动真实 viewerd，断言 pane 可挂载 + 三个 tab 的 RPC 通道可用）。

## 硬约束（invariants）

1. **后端 Go 插件不拆**：`viewer.chat` 仍是唯一后端 chat 插件；chat-manager 是纯前端模块，只经既有/扩展的 `chat:_:*` RPC 消费。
2. shell 契约零破坏：DockProvider 字段只加不删（pin 是 shell 侧行为，不动 provider 接口签名）；非 singleton 插件（terminal/files）行为不变。
3. 数据面三层（turn_events → message_blocks → messages）不动；relay/LLM router/failover/Hindsight 行为不变，只换 roles/routes 的读取来源。
4. agent 协议库（internal/acp、internal/codexserver）零修改——turn_id 只出现在插件契约帧 payload。
5. 禁裸 SQL（GORM only）、禁 CGO；不引入新依赖，除非不可替代并在报告里说明。
6. 测试里 agent provider 永远 mock（`scripts/mock_acp_agent.py` / `scripts/mock_codex_server.py`），不在门禁里起真 hermes/codex。
7. go:embed 坑：改前端后跑 `web/build-release.sh`（它会编译并恢复 web/dist 占位符），不要手改 web/dist。

## 门禁（全部亲自跑绿才算完）

```bash
export PATH=$HOME/.local/go/bin:$PATH
cd next-go
gofmt -l .                    # 必须空
go build ./...
go vet ./...
go test -count=1 ./...
go test -race -count=1 ./internal/broker ./internal/kernel ./internal/plugins/chat ./internal/agentdriver
scripts/smoke_all.sh          # 必须 9/9
cd ../next/ts-sdk && npm test # 必须全绿（现 24/24，新用例加上去）
cd ../frontend && npx vue-tsc --noEmit && npm run build
```

完成后报告：每个工作项的改动文件清单、门禁原始输出尾部、新增 RPC/channel 清单、storage 迁移逻辑摘要。**不要 git commit**——Hermes 复核后统一提交。
