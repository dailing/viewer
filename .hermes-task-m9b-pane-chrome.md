# M9b: Pane chrome 注册机制 + chat dock 实例过滤

> 任务规格（Hermes 编写，Codex 执行）。权威设计：docs/plugin-framework.md v0.25（header 变更行 + §8.8 + §18，已提交）。纯前端改动，Go 侧零改动。

## 已核实现状（viewer-plugin 工作树 @ 现 HEAD）

- `next/frontend/src/shell/PaneContainer.vue`：已有 pane title bar（图标 + 自动标题 `{provider.title} #{instanceId}` + 标准按钮 refresh/split/split/close）。**无插件自定义机制**。
- `next/frontend/src/shell/PluginPaneHost.vue`：创建 `ctx = createCtx(uid, instanceId)` 并 provide("pluginCtx")，卸载/切换时 `ctx.dispose()`。
- `next/frontend/src/shell/ctx.ts`：PluginCtx 定义处（bus / instanceId / dispose）。
- `next/frontend/src/plugins/chat/ChatPane.vue:22`：**自渲染第二条 header**（chat 名 + bi-sliders 按钮 → `layout.openInstance("chat-manager","main")`）——待拆除。
- `next/frontend/src/plugins/chat/index.ts`：dock provider `instances` = `chats:list` 全量（13 个 chat 全上 dock）——待收窄。
- **移植源（只读参考，勿改）**：老版 `/mnt/oldroot/home/d/Sync/viewer/frontend/src/stores/paneToolbar.ts`（PaneToolbar 类型 + pinia store）与 `components/PaneTitleBar.vue`（渲染方式：title/status 文本/actions 按钮组/select 控件/chips）。

## 工作项

### W1 — paneChrome store + ctx.setChrome（shell 契约扩展）
1. 新建 `next/frontend/src/stores/paneChrome.ts`：pinia store，keyed by content uid（`paneType:instanceId`）。类型直接移植老版 paneToolbar：
   - `PaneChrome = { title?, status?, statusClass?, actions?, controls? }`
   - `PaneChromeAction = { id, title, icon?, label?, active?, variant?: "default"|"danger", run: () => void|Promise<void> }`
   - `PaneChromeControl = select {id,title,value,options,size?,onChange} | chips {id,title?,items[]}`
   - actions：`setChrome(uid, chrome)` / `clearChrome(uid)`；getter `forUid(uid)`。
2. `ctx.ts`：PluginCtx 增加 `setChrome(chrome: PaneChrome): void`——内部调 store.setChrome(本 uid)；`dispose()` 时自动 clearChrome。createCtx 需要拿到 uid（已有第一个参数）。
3. `PaneContainer.vue` title bar：
   - 标题：chrome.title ?? 现有自动标题逻辑。
   - chrome.status 渲染为标题旁小字状态（class 用 statusClass）。
   - 标准按钮**之前**插入 chrome.actions（icon 按钮，active/variant 样式参考老版 PaneTitleBar.vue）与 chrome.controls（select / chips）。
   - 现有 refresh/split/close 按钮保持不变。
4. 响应性：chrome 变更须即时反映（pinia store 天然响应）。

### W2 — ChatPane 拆除内部 header，改用 chrome
- 删除模板中 `<header class="d-flex ...">` 整行及其按钮。
- `onMounted`/`load()` 后注册 chrome：`ctx.setChrome({ title: chat?.name ?? "Chat", actions: [{ id: "chat-config", title: "聊天管理", icon: "bi-sliders", run: () => layout.openInstance("chat-manager", "main") }] })`。
- chat 名在 `load()` 完成后才已知——load 后 setChrome；若后续 rename（manager 里改名）需刷新：订阅 `chat:_:active` 或直接也在 `chat:_:chats` 变更事件上重取（按现有事件面选择最小实现；无合适事件则 load 时设置即可， rename 后手动刷新 pane 可接受，但要在代码注释说明）。
- `openManager` 函数若不再被模板引用则删除。

### W3 — chat dock 实例过滤（pinned ∪ 已开）
- `chat/index.ts` createDockProvider：`instances` 从"全量 chats"改为过滤：**`chat.pinned === true` 或 `layout.isUidOpen("chat:"+chat.id)`**。
- 注意响应性：layout 开关 pane 时列表须即时更新（用 getter/computed 暴露 instances，或 watch layout 状态触发重算）。保持 DockProvider 契约类型（`instances: readonly DockInstance[]`）不变。
- pinned 项 label 保留 `★ ` 前缀。
- `create` 流程不变（新建后 openInstance 打开 → 已开状态使其出现在 dock）。

### W4 — 顺带检查
- 其他插件（terminal/inspector/files/chat-manager）若也有自渲染标题栏，一并拆除改 chrome（只限标题栏性质；面板内部 tab 栏如 ManagerPanel 的 nav-tabs **保留不动**）。
- 空 pane 占位文案不变。

## 禁止项
- 不改 Go 后端、不改 RPC/帧契约。
- 不改老版 `/mnt/oldroot/home/d/Sync/viewer`（只读参考）。
- 不动 Dock.vue 的 singleton pin 逻辑（v0.24 已定）。

## 门禁（全部通过才算完成）
```bash
cd next/frontend && npx vue-tsc --noEmit && npm run build
cd next/ts-sdk && npm test   # 25/25 不回退
# Go 侧无改动，但回归确认：
export PATH=$HOME/.local/go/bin:$PATH
cd next-go && gofmt -l . && go build ./... && go vet ./... && go test -count=1 ./... && scripts/smoke_all.sh 9/9
```
不要 git commit（由 Hermes 审阅后提交）。
