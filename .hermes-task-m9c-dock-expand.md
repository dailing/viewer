# M9c: Dock hover 自动展开（显示条目名）

> 任务规格（Hermes 编写，Codex 执行）。权威设计：docs/plugin-framework.md v0.26（§8.8 最后一条）。纯前端改动，只动 `next/frontend/src/shell/Dock.vue`（样式变量若有需要可动 `styles.css`），Go 侧零改动。

## 已核实现状

- `next/frontend/src/shell/Dock.vue`：dock 为纯图标窄条；条目按钮只有 icon，全文在 `title` tooltip。singleton 条目有 hover 才出现的 pin 切换按钮；instance 条目 label 形如 `"★ Viewer Maintain · /mnt/.../viewer"`（chat）或 provider title（singleton）。
- pin 状态持久化在 localStorage（`viewer.dock.pinned`，按 provider type 键控）——**不动**。

## 工作项

### W1 — Dock hover 展开
1. dock 默认保持现在的纯图标窄宽度。
2. **hover 意图计时**：`pointerenter` 进 dock 起 500ms 定时器 → 加 `expanded` 状态；500ms 内 `pointerleave` 则取消。展开后 `pointerleave` 立即收回（清定时器 + 去 expanded）。组件卸载时清理定时器。
3. **展开态渲染**：dock 宽度展开（建议 ~220px，CSS transition 平滑），每个条目图标旁显示文本：
   - singleton：provider title（如"聊天管理"）。
   - instance：显示 chat 名——label 里 `★ name · /长/路径` 的 root 部分在展开态**截断只显示 basename**（如 `viewer`、`pami_pp`），完整 label 仍保留在 tooltip。实现上建议在 dock 渲染处从 label 派生短文本，不改 DockInstance 契约；若你认为给 DockInstance 加个可选 `shortLabel` 更干净也可以，二选一，别都做。
   - 文本 overflow ellipsis，不换行。
4. 展开时条目按钮变为横向 flex（icon 左 + 文本右）；pin 按钮行为不变（hover 出现，展开时挤占空间要自然）。
5. 展开态下 tooltip 可保留（信息更全），不做禁用。
6. 无开关、不持久化、不影响键盘/触摸既有行为。

### W2 — 顺带
- 确认展开/收回不引起 pane 区抖动以外的布局错乱（dock 是布局内元素，宽度变化会推 pane——这是预期行为，transition 平滑即可）。
- `architecture.md` 的 dock 描述顺手更新一句（hover 展开）。

## 禁止项
- 不改 Go 后端、不改 RPC/帧契约、不改 DockProvider/DockInstance 既有字段语义。
- 不动 singleton pin 制（v0.24）与实例过滤（v0.25）。

## 门禁
```bash
cd next/frontend && npx vue-tsc --noEmit && npm run build
cd ../ts-sdk && npm test   # 25/25 不回退
```
不要 git commit（由 Hermes 审阅后提交）。
