[project]
name = "viewer-next"
version = "0.1.0"
description = "Viewer bus SDKs and prototype frontend; the kernel and core plugins live in next-go/"
requires-python = ">=3.12"
dependencies = [
    "websockets>=15",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.26",
]

[tool.uv]
package = false
